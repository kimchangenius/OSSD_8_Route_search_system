name: Ping Render

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend health
        run: |
          curl -sS --max-time 10 https://ossd-8-route-search-system.onrender.com/api/health >/dev/null || true
